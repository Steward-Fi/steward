{
  "name": "steward",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/examples/*",
    "web"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "seed": "bun packages/seed/src/index.ts"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
