{
  "name": "steward",
  "private": true,
  "workspaces": [
    "packages/db",
    "packages/*",
    "packages/webhooks",
    "packages/sdk",
    "web"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
